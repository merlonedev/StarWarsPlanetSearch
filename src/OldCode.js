// switch (filter) {
// case 'nome':
//   return setPlanetList(planetList.filter((each) => each.name === data.name));
// case 'population':
//   return setPlanetList(
//     planetList.filter((each) => each.population, condition, number),
//   );
// case 'orbitalPeriod':
//   return setPlanetList(
//     planetList.filter((each) => each.orbital_period, condition, number),
//   );
// case 'diameter':
//   return setPlanetList(
//     planetList.filter((each) => each.diameter, condition, number),
//   );
// case 'rotationPeriod':
//   return setPlanetList(
//     planetList.filter((each) => each.rotation_period, condition, number),
//   );
// case 'surfaceWater':
//   return setPlanetList(
//     planetList.filter((each) => each.surface_water, condition, number),
//   );
// default:
// setLoading(false);
// return (
//   setPlanetList(data)
// );
