const helperToOptions = {
  columnFilter: [
    'population', 'orbital_period', 'diameter', 'rotation_period', 'surface_water',
  ],
  comparison: ['maior que', 'igual a', 'menor que'],
};

export default helperToOptions;
